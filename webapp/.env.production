# Production API base URL — points to the Render-deployed FastAPI backend.
# Update this if the Render service name is changed (the URL is
# https://<service-name>.onrender.com).
VITE_API_URL=https://openpkflow-api.onrender.com
